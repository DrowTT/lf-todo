# Repo-Local Skills

这个目录存放 LF-Todo 仓库专属的 skill。

当前约定：

- `skills/multi-agent-dev/`：多 agent 标准开发流程
- `skills/multi-agent-evolve/`：多 agent 自我进化与经验沉淀

目录职责：

- 只放仓库专属、需要随仓库版本管理的 skill
- 不放 Codex 角色模板或运行配置
- 旧的 `.agent/` 历史目录已退役，不再使用

如果后续新增项目专属 skill，优先放在这里。
